# Multi_Session_Tmux_Interactive
This script allows you to interactively set up multiple sessions and multiple windows per session using tmux and saves it to a state. Reusable and increases productivity.
